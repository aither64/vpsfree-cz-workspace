{
  "checks": [
    {
      "check": "initial native handoff interrupted; continuing the exact same thread"
    },
    {
      "check": "real prompt reads private uploaded file bound at session creation",
      "codexVersion": "0.154.0"
    },
    {
      "check": "package rollback retains bytes and rollforward restores attachment metadata"
    },
    {
      "check": "real fork retains the original attachment without copying bytes"
    },
    {
      "check": "fork reads the shared file in its own completed turn"
    },
    {
      "check": "archive and revive retain the original input and conversation"
    },
    {
      "check": "confirmed sent deletion affects both real fork references"
    },
    {
      "check": "real session deletion completes owner cleanup before journal finalization"
    }
  ],
  "package": "/nix/store/piif11vnfq24is20gpmib5i40kb66k9h-dev-workspace-0.2.0",
  "exitCode": 0,
  "cleanup": "Both private sessions deleted; all owned App Server, portal, tmux, proxy and metadata fixtures removed."
}
