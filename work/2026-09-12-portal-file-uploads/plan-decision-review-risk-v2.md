## Blocking

- Legacy recovery still reproduces the original identical-plan collision for an already-open pre-upgrade page. The parent browser `9ed5ff5` matches attempts using only `plan:<digest>` and therefore reuses plan A’s client ID for a later identical plan B. The updated handler reconstructs that same legacy context and calls `ReconcileSend` before reading or validating the current plan ([server.go:1574](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/dev-workspace/portal/internal/web/server.go:1574)); an accepted attempt is returned immediately by the pinned provider ([client.go:3750](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/codex-web/codex/client.go:3750)). The old page consequently treats B as implemented although only A was sent. The regression covers later turns only for v2 identities, while the legacy test uses the same plan turn ([plan_decision_test.go:75](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/dev-workspace/portal/internal/web/plan_decision_test.go:75)). Legacy recovery must fail closed whenever it could be confused with a currently eligible plan—for example by checking transcript/turn ordering before returning the receipt—and needs an old-page/new-server identical-plan regression.

## Important

- Rejected legacy prepared attempts have no supported disposition and can permanently block normal archival. `ReconcileSend` deliberately leaves `prepared` records untouched ([client.go:3732](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/codex-web/codex/client.go:3732)), after which the runtime returns the reload conflict ([server.go:1590](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/dev-workspace/portal/internal/web/server.go:1590)). Reloading and submitting a fresh v2 identity does not remove the legacy record, and transcript acknowledgement cannot remove a message that was never submitted. The provider then rejects retirement readiness for every non-accepted record ([client.go:2246](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/codex-web/codex/client.go:2246)). This state can arise from the existing prepare-then-settings-update partial-failure window ([server.go:1613](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-file-uploads/dev-workspace/portal/internal/web/server.go:1613)). Add a safe, exact cancellation/migration path for legacy prepared attempts and test that retirement remains possible.

The revision JSON and committed flake pins consistently select runtime `2bfb0ee6` and provider `c0fbae9d`; no pin drift was found. No Advisory findings.