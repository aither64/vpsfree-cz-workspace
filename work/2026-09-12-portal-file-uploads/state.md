---
lifecycle: active
---

# Portal file uploads state

## Status and authorization

The user authorized implementation and deployment to aitherdev through
vpsfree-cz-configuration. Implementation, mandatory review, browser and real Codex acceptance, CI and
deployment are complete. The tested package and configuration are live on
aitherdev. No implementation or deployment blocker remains.

All feature branches remain unmerged. No default-branch integration, real-session
archive, delete or stop is authorized. Preserve this initiative for follow-up.
Initial plan/state were committed as 94ca59f3f9d17306c2cc5e61099d83313309f4df before
project commits or external mutation. Shared checkout remains on master with
unrelated coordination changes preserved.

## Repositories

Every project uses branch 2026-09-12-portal-file-uploads and worktree
worktrees/2026-09-12-portal-file-uploads/<project>. All five are registered in
portal.yml. Final committed and pushed feature heads:

| Project | Head |
| --- | --- |
| codex-web | `7b79942eee2d7bcaf52252249d8599db76c033b2` |
| dev-workspace | `30bfb2a378138a7fdfe8f6aaa05fcbf8d98037b3` |
| vpsfree-dev-workspace | `9db07ad92eeb62490dbb14cdb5b9cd9a47b4412c` |
| workspace | `fb3399804582b8bee110845c40b8ab3bac3893e3` |
| vpsfree-cz-configuration | `63652724f9ed0202d6f9c842d842da89ab33bbaa` |

Bases: codex-web de83e9c72dec6cb5ff8ec13d5b0b21ed60148117;
dev-workspace dc8d6cf7628a6a8db240f2e11ad61e8bc31ddacd;
organization b37edd0f63fb7a984ba634c2d52d08e9344304b4;
configuration b4e120294696578c3871124259f266205bad4393.
Workspace feature was rebased onto shared master
56b9b7236f6f16f127461ef0f878b295b34e1ca7 before final review. Explicit fetches of
remote master found no further project changes. Remotes use SSH.

Provider-to-consumer pins use committed pushed revisions. Configuration pins
are generated by confctl inputs channel set --commit; generated messages remain
unchanged. The measured provider vendor hash is
sha256-UX1zJ8pxrc2CyvZBVK2HdNF3Pp0vu/vXXRtDOlCfBiU=.

## Implemented behavior

Picker/drop, progress, retry/resume, removal, attachment-only initial and ordinary
prompts, Send/steer/Queue, transcript and queue cards. Defaults: 1 GiB/file,
10 files and 2 GiB/prompt, 10 GiB/session, 100 GiB/workspace, 4 MiB chunks,
2 browser workers, 8 server chunks and 1 GiB free-space reserve. Private upload
catalog and bytes live outside workspace Git. Seven-day expiry affects unused
input; sent and unresolved input is retained. Forks share references; archive
and revive preserve input; explicit owner-session deletion reclaims bytes.

No App Server protocol, existing lifecycle journal or receipt format changes.
Rollback retains bytes and text prompts. Resolve queued deletion intents before
rollback: an older package can clear its receipt without completing provider
cleanup, leaving safe retention until explicit owner-session deletion.

## Review and verification

High risk: private persistent state, deletion, authorization, cross-project
contracts and deployment/rollback. General, architecture, scope and risk lanes
all completed with fresh gpt-5.6-sol xhigh reviewers. See review-packet.md,
review-*.md and review-reconciliation.md. Blocking/Important findings were fixed;
the retention-only rollback boundary is explicitly accepted in the plan.

Architecture/risk reruns reviewed the new optional queue completion contract at
review-packet-v2.md heads. Their duplicate Important finding was fixed with POST
queue/reconcile under normal mutation authority and shared locks; GET is
observational. Direct fixes were checked without unnecessary reviewer reruns.

- Provider full Go suite and four Node contracts pass at 7b79942. Targeted queue
  authority/concurrency race check passes. Provider CI 34721458290 passed.
- Final runtime full Go suite passes at 30bfb2a. Focused race tests cover initial
  adoption after fork/restart, repeated prompt identity and deletion, metadata
  limits, creation acceptance/deletion and queue observation ordering.
- Ruby removal/fork suite: 69 tests, 622 assertions, pass. CLI tests use the real
  store for cleanup identity and journal retry. Intermediate runtime functional
  commits were independently compiled after splitting the original bundled commit.
- Browser Firefox 155/Selenium acceptance: 13 checks pass, including real picker,
  drop, resume after reload/reselection, Send, steering, Queue/removal, busy and
  idle deletion, attachment-only initial form and 390px layout. A full 1 GiB
  transfer took 35.45 seconds with matching SHA-256 and 55,533,568 bytes peak
  server RSS. See browser-results.json and screenshots. Confirmation calls were
  instrumented; native dialog UI was not independently verified.
- The browser found missing client forwarding and compatibility routing for
  queue reconciliation. Both have shipped-client HTTP regressions.
- Real isolated Codex 0.154.0 read the private uploaded file. Rollback/rollforward
  retained exact bytes and restored attachment cards. Fork inherited the same
  input without a copy. Subsequent archive exposed inherited initial records
  confusing adoption; 30bfb2a fixes this and adds restart/foreign-owner checks.
  The same fixture exposed repeated initial text rebinding; item identity now
  stays stable. Final lifecycle acceptance passed on the exact deployed package,
  including reading shared input in a fork, archive/revive, confirmed shared-file deletion
  and actual deletion of both disposable sessions. See live-results.json.
- Real lifecycle fixture setup refusals (PTY confirmation, committed active
  lifecycle) were investigated and corrected. These are fixture errors.
- Final Nix package and aitherdev system builds passed. Exact site/pin deployment
  contract passed at 30bfb2a. Dry activation and switch of configuration generation
  2026-09-13--00-25-38 passed, including both configured system health checks.
  No local kernel build ran.
- All current-head CI passed: provider 34721458290, runtime 34722649380,
  organization 34722676209 (flake and devcluster checks). See ci-results.json.
  Superseded organization run 34722038395 was cancelled; 34722246756 had already
  finished successfully when cancellation was attempted. An earlier runtime
  cancellation returned HTTP 403 and was allowed to finish; it is not used as
  current-head validation. Workspace/configuration have no feature CI workflows.

Use the flake-derived Go/gcc/Node/Ruby environment, then explicitly set
TMPDIR=/tmp GOWORK=off GOFLAGS=-mod=mod. The long Nix-shell TMPDIR breaks Unix
socket fixtures. Early untracked provider development used an explicit tooling
shell because buildGoModule could not see untracked source. Final validation
uses the actual pinned provider and packaged output. Configuration hooks pass;
the generated confctl commit message has the expected width warning.

## Session startup recovery

The initial bootstrap hit the existing exact-cwd thread/list history timeout
(60 seconds, 2,365 historical threads). This unfinished creation journal blocked
workspace-host switch. The original coordination-only goal was recovered from
this conversation's own command history and its digest verified. Current tracking
was preserved; original plan/state bytes from 94ca59f were restored only while
retrying the exact dev-session start request, then current tracking was restored.
The first retry timed out; after read-only history lookups (43–45 seconds), the
second normal CLI retry completed and cleared the guard. No journal proof was
edited, guard bypassed, replacement conversation allocated or shared App Server
restarted. The lookup cost remains an upstream operational limitation.

The initiative now has shared thread 01a097c4-a309-7f00-8b6b-dc76db2bb13e, a ready
manifest/journal, all five worktree registrations and curated artifacts. Matching
DEV_SESSION_SLUG and DEV_SESSION_WORKSPACE were verified with dev-session current.
The original goal is retained as startup-goal.txt for recovery evidence.

Isolated Codex 0.154.0 native startup interrupted the first test turn with the
same CODEX_HOME throughout. The exact same conversation then read its uploaded
file through ordinary Send. A disposable fork without its own turn was also
unavailable to the final retirement lookup; the final fixture exercised a real
fork turn and both deletions succeeded. These observations concern existing
Codex startup/materialization, not attachment transfer. See
notes/dev-workspace/2026-09-13-upload-acceptance-fixtures.md.

## Deployment and cleanup

Previous installed package:
/nix/store/8ivw1xqshnhk12qwgkvwwqql4jm5d851-dev-workspace-0.2.0.
Previous system:
/nix/store/nlqpccihvw82h26wr2g425nz3s16axlj-nixos-system-aitherdev-26.05.20260910.d58a46e.

Deployed user profile 33:
/nix/store/piif11vnfq24is20gpmib5i40kb66k9h-dev-workspace-0.2.0.
Deployed system (confctl generation 2026-09-13--00-25-38):
/nix/store/fllksyvh2nr52am87vs6nj7czwsnl19i-nixos-system-aitherdev-26.05.20260911.21a67dc.

Normal workspace-host switch --source selected the package. Confctl deployment
ran from the configuration feature worktree. Live authenticated HTTPS passed an
8 MiB upload in three chunks, each checksum, completion, exact download and draft
removal; unauthenticated health returned 401. Health was rechecked after system
activation. Portal, router, nginx and Codex services are active. Codex remained
0.154.0 and its MainPID stayed 1090021 throughout deployment. Final page checks
found a duplicate built-in plan.md artifact in this initiative manifest; removing
it restored normal manifest validation. The index, initiative page (including
its upload composer) and shared upload browser module all return 200.

All owned fixture processes, temporary metadata homes, test TLS keys and private
workspaces were removed. The temporary Go browser test is removed from the project.
Curated scripts/results/screenshots are retained; bulky failure captures and
Python caches were removed. Built Nix outputs and configuration generations remain
available for follow-up deployment/rollback. Project tracked worktrees are clean.

Next operator action is optional acceptance/integration of the retained feature
branches. Keep all worktrees, branches and the initiative open; no archive or stop
is scheduled. See deployment-results.md for the compact deployment evidence.

Stable portal:
https://vpsfree-cz.workspace.aitherdev.int.vpsfree.cz/2026-09-12-portal-file-uploads/
