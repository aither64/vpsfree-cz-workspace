## Findings

1. **Blocking — A completed plan-created session can remain permanently hidden after its source is archived or deleted.**  
   Commit `1de0a78` records the destination creation journal as ready before writing receipt-bound completion evidence ([dev-session](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/libexec/dev-session:938)). A crash between that point and the evidence write leaves a valid destination with a paused receipt. Reconciliation deliberately treats matching goal/settings as recoverable rather than conflicting ([creation.go](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/portal/internal/web/creation.go:183)), but retry then revalidates the source even when the plan receipt was already validated ([creation.go](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/portal/internal/web/creation.go:455)); the CLI repeats that check before examining the ready start journal ([dev-session](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/libexec/dev-session:612)). An archived source is explicitly rejected. Consequently, the receipt remains blocking while the completed canonical destination cannot be opened or recovered.  
   Recovery should use the immutable validated plan/goal/settings and matching destination journal without requiring a live source, or reconciliation must release the canonical destination as a conflict when recovery is impossible. Add crash-boundary regressions for both archived and deleted plan sources.

2. **Important — No-watch activity reads retain unbounded history in memory while repeatedly discarding the corresponding summary cache.**  
   `readAllTurns` installs an authority-lifetime history entry for every thread and may retain 100,000 per-turn metadata maps ([turn_history.go](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/codex-web/codex/turn_history.go:35)). The only deletion is in `removeWatch`, reached when a subscription is removed ([client.go](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/codex-web/codex/client.go:963)). `ReadActivity` itself does not retire this cache. This matters in the portal because archived sessions are intentionally excluded from monitor subscriptions ([activity.go](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/portal/internal/web/activity.go:116)) but still receive a read-capable activity provider, and the browser polls it every five seconds without checking `interactive` ([app.js](/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-12-portal-review-experience/dev-workspace/portal/internal/web/static/app.js:1637)). Sequentially viewing large archived histories therefore grows the observer client indefinitely. Meanwhile, recorder `release` closes each no-watch thread cache, so every subsequent poll reopens per-turn summary files during aggregation.  
   Give history caches a bounded/ref-counted lifecycle and avoid continuous activity polling for archived/noninteractive sessions. Coverage should include repeated no-watch reads across multiple threads and an archived browser session.

## Review assessment

No additional findings.

The four worktrees are clean. The final histories contain focused non-merge series of 2, 7, 1, and 1 commits; subjects and bodies conform to repository rules, and the small subject-only renderer-consumer commit is proportionate. Remediations are folded into their owning commits without abandoned branch-only formats or fixup history.

The dependency chain is coherent: Go module, Nix source, organization wrapper, and consuming workspace resolve `codex-web` `4dde3c6`, `dev-workspace` `40837d2`, and `vpsfree-dev-workspace` `4e03a3f`; the Codex version and CodeMirror graph remain unchanged. Documentation describes the final bounded receipt, repository-review, and activity behavior.

Per instruction, I ran no tests. The packet reports substantial focused/race/CI coverage, but the two lifecycle combinations above are not represented. Packaged VM, actual App Server/browser creation, and live upgrade/rollback remain appropriate post-review gates.