# Deployment verification

Profile29 is installed from workspace `9edf553`:
`/nix/store/prpvck3v35xbvyz60gfwprz9p3lsagal-dev-workspace-0.2.0`.
It selects runtime `d3bfd0f`, provider `8377021` and organization `a30de6c`.
The final package includes recovery for approved plans with trailing whitespace.
The same real failed receipt recovered before deployment without a new thread,
CLI invocation, attempt or initial submission; its frozen plan and evidence
remained unchanged.

`workspace-host switch` passed the package-generation, cluster/runtime and
Codex 0.154 protocol/model preflights. Authenticated HTTPS health/session checks
pass; unauthenticated health access returns 401. All four repository histories
and complete branch comparisons return files, the browser assets are served,
and activity resolves this initiative's exact thread. Router, portal, Codex and
tmux services are active. Sanitized checks are in `live-feature-checks.json`.

Profile28 retains the prior feature package `pwi8v7s`; profile27 retains the
pre-feature package `aamx7bq`. No system configuration or default branch changed.

## Live rollback boundary

The earlier `workspace-host rollback` from profile28 passed old-package Codex
compatibility but refused before profile mutation because another session had
an in-progress turn. The helper restored terminal clients and left profile28
healthy. No forced interruption or repeated rollback attempt was made.

The isolated new-to-old-to-new package fixture passed: the old package read
all canonical sessions and replayed the source without duplicate submission;
rolling forward preserved a separately completed conflicting destination.
These checks did not interrupt concurrent work. An actual registered-profile rollback requires
an idle workspace and is not claimed here. The later forward switch to profile29
succeeded through the ordinary host helper and passed HTTPS/service checks.
