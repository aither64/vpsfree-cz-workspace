# Email refinement review packet

Review this user-requested follow-up directly, using the mandatory review skill
and your assigned lane. Do not launch nested agents. Do not mutate project
code, push branches, run long integration tests or operate session lifecycle.
Write findings to `review-email-LANE.md` beside this packet.

## Requested result and scope

The user accepted this follow-up plan and requested implementation:
- Czech warning: “Pokud ses o přihlášení nepokoušel ty, je možné, že tvoje heslo
  zná někdo jiný.” Keep password-change/support advice.
- Remove resend mechanics from verification emails in both languages.
- Match production new-device notification's service/time/device/IP/PTR layout;
  reuse the existing readable user-agent formatter and DNS lookup.
- Add Czech/English HTML variants of verification mail only, in the built-in
  and production repositories, preserving visible parity and escaping values.
- Suppress the separate unknown-device notice only after successful email-code
  verification, including a forced password change. Keep password-only/MFA
  notices and mark devices known only after successful session creation.

No new auth protocol, schema, trust rule, code lifetime/rate limit, node change,
new-device notification HTML, deployment or production wiki publication.
The original larger email feature was reviewed and reconciled already; review
this follow-up delta and relevant context, not a fresh unconstrained redesign.
See prior `review-packet.md`, lane reports and `state.md` for background.

## Repositories and commits

Worktree root: `/home/aither/workspace/ai/vpsfree.cz/worktrees/2026-09-13-auth-email/`.
All branches: `2026-09-13-auth-email`; all intended project changes committed.
Tracking: `/home/aither/workspace/ai/vpsfree.cz/work/2026-09-13-auth-email/`.

- vpsAdmin follow-up base (rebased prior feature): `cf2c8734c9091327cbc9157d43915695e9750bc5`
- vpsAdmin mail presentation/metadata: `95a67e55e9695b5a7f56b491af186840c389f444`
- vpsAdmin final notification suppression head: `d6f51b6e8ad9d78c68e6d0f0ce5276ca610e7bf8`
- vpsAdmin upstream base: `5ac49802f892da6fd37a88c8349b9cc7ea826538`
- Production templates base: `06f03bad4294b6f28ca9478e907f43967ebeb7d0`
- Production templates head: `97ac666a390fc028c5b610b48c9963adb9e0b4ef`
- KB contract currently `cdc4fa967ad1b8e9daa665e6760db624e736c1fd`; only a mechanical final API pin remains after browser validation. Preserve OS pin `6bdf458fd9105379860234ff33d352e55844f08f`.

The two API commits deliberately separate independently reviewable mail
presentation from notification suppression. Production templates have one
matching commit. Rebase onto upstream 5ac49802f was conflict-free; upstream
changes are only io-console 0.9.3 and PHP parser 5.9.0 dependency updates.

## Implementation and compatibility

`EmailLogin.start` snapshots the validated OAuth client's name (token default:
`vpsAdmin API`), user agent, initial IP and PTR, and request time from AuthToken.
Resends use those stored details. DNS resolves before shared account/IP budget
locks. Mail registration adds service_name/requested_at/device/ip_ptr, retains
existing variables and exclusive-primary delivery. Built-in/production HTML
uses the password-recovery email style and exact standard footer.

`NewOAuth2Login` checks the persisted authorization evidence's method `email`
only for deciding the notice, after existing authority checks at code exchange.
Device-known update and session creation remain in their existing locations.
Tests exercise notification-enabled success, MFA/password behavior, browser
binding and forced-reset completion.

New API supports older text templates. All API workers must supply new variables
before revised production templates are installed. This feature has never been
merged or deployed; there is no supported intermediate-branch challenge format
or data migration to preserve. Existing queued mails are already rendered.
Rollback of this follow-up restores the duplicate notice; template rollback is
compatible. No production changes are authorized.

## Quick verification

- Initial focused API suite: 96 examples, zero failures.
- Final mail suite after formatter coverage: 4 examples, zero failures.
- Rebased final focused suite: see `/tmp/auth-email-refinement-final-specs.log`;
  96 examples passed after rebase (seed 49818). The final subsequent fixture-only adjustment explicitly enables mail for the browser account and passed Nix parsing/hooks.
- Ruby lint and all commit hooks pass; one corrected hash alignment. Hook text
  width advisories use 72 columns; repository permits 80.
- JS syntax and WebUI Nix parsing pass.
- Built-in checker: 55 templates / 179 files. Production `nix flake check` passes.
- All 4 synthetic language/repository previews render with exact visible
  plain-text/HTML parity: `mail-previews/`, generated by
  `render-mail-previews.rb` using the actual API time-zone formatter.
- Runtime mail specs cover primary-only recipients, missing template failure,
  unknown user-agent HTML escaping, missing PTR and readable Firefox/Linux.
- No new spec files or selectors: existing recursive API topics still cover
  them; focused browser target remains `webui#auth-email`.
- Focused browser integration starts only after review reconciliation.

## Review classification

High risk: authentication-related security notification behavior, untrusted HTML
values, persisted pending-auth metadata and the API/template deployment contract.
Required lanes: GENERAL, ARCHITECTURE, RISK/COMPATIBILITY, SCOPE. Every reviewer
must use fresh gpt-5.6-sol/xhigh. Main agent owns user-facing writing and applied
both installed language skills before committing; reviewers may inspect the
finished Czech/English prose and synthetic previews.
